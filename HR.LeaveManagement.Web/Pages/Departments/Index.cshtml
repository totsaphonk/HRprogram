@page
@model HR.LeaveManagement.Web.Pages.Departments.IndexModel
@{
    ViewData["Title"] = "Departments";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-md-12">
            <div class="d-flex justify-content-between align-items-center mb-3">
                <h2>Department Management</h2>
                <a class="btn btn-primary" asp-page="Create">Add New Department</a>
            </div>
            
            <div class="card">
                <div class="card-header">
                    <h5>Department List</h5>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Name</th>
                                    <th>Description</th>
                                    <th>Manager ID</th>
                                    <th>Employee Count</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var department in Model.Departments)
                                {
                                    <tr>
                                        <td>@department.DepartmentID</td>
                                        <td>@department.Name</td>
                                        <td>@department.Description</td>
                                        <td>@(department.ManagerID?.ToString() ?? "Not Assigned")</td>
                                        <td>@department.Employees.Count</td>
                                        <td>
                                            <a class="btn btn-sm btn-outline-primary" asp-page="Details" asp-route-id="@department.DepartmentID">View</a>
                                            <a class="btn btn-sm btn-outline-warning" asp-page="Edit" asp-route-id="@department.DepartmentID">Edit</a>
                                            <a class="btn btn-sm btn-outline-danger" asp-page="Delete" asp-route-id="@department.DepartmentID">Delete</a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
